depend() {
	config "$CONFD"
}

start() {
	ebegin "$STARTD"
	ip link set $interface up
	ip addr add 0.0.0.0 dev $interface
	eend $?
}

stop() {
	ebegin "$STOPD"
	ip link set $interface down
	eend $?
}
